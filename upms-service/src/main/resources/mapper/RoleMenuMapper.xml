<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auth.center.role.infrastructure.repository.mapper.RoleMenuMapper">

    <!-- 批量插入角色菜单关联 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO sys_role_menu (tenant_id, role_id, menu_id, created_by, created_time)
        VALUES
        <foreach collection="roleMenuList" item="item" separator=",">
            (#{item.tenantId}, #{item.roleId}, #{item.menuId}, #{item.createdBy}, #{item.createdTime})
        </foreach>
    </insert>

</mapper>